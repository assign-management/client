import { IconButton, styled } from '@mui/material';

export const ProjectDeleteHeaderButton = styled(IconButton)`
  padding: 0.7rem;
  border-radius: 0.6rem;
`;

ProjectDeleteHeaderButton.defaultProps = {
  size: 'small',
};
