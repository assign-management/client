import { styled } from '@mui/material';

export const PasswordRecoveryTitleMain = styled('h2')`
  && {
    font-size: 2.3rem;
    font-weight: 700;
    margin: 0;
    margin-bottom: 1rem;
    color: #4f4f4f;
  }
`;
