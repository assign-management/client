import { styled } from '@mui/material';

export const LoginLinkWrapper = styled('p')`
  text-align: center;
  text-transform: capitalize;
  font-size: 1.5rem;
  margin: 0;
  margin-top: 1.5rem;

  a {
    padding: 0.5rem;
  }
`;
