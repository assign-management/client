import { styled } from '@mui/material';

export const RegistrationHeaderTitle = styled('p')`
  margin: 0;
  font-size: 1.4rem;
  color: #fff;
  font-weight: bold;
  letter-spacing: 0.05rem;
  line-height: 1;
`;
