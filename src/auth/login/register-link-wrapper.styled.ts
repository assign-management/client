import { styled } from '@mui/material';

export const RegisterLinkWrapper = styled('p')`
  font-size: 1.5rem;
  margin: 1rem 0;
  text-align: center;
  text-transform: capitalize;

  a {
    padding: 0.5rem;
  }
`;
