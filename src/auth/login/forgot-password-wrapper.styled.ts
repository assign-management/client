import { styled } from '@mui/material';

export const ForgotPasswordWrapper = styled('p')`
  margin: 0;
  font-size: 1.5rem;
  font-weight: bold;
  text-align: center;
  text-transform: capitalize;

  a {
    text-decoration: none;
  }
`;
