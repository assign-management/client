import { CircularProgress, styled } from '@mui/material';

export const BoardLocationProgress = styled(CircularProgress)`
  margin-left: 1rem;
`;

BoardLocationProgress.defaultProps = {
  color: 'inherit',
  size: 15,
};
