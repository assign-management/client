import { styled } from '@mui/material';
import { IconButton } from '../../common/buttons';

export const CancelSubmitButton = styled(IconButton)`
  && {
    font-size: 1.8rem;
    margin-bottom: 1rem;
    margin-left: 1rem;
  }
`;
