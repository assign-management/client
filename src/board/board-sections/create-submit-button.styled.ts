import { styled } from '@mui/material';
import { SubmitButton } from '../../common/buttons';

export const CreateSubmitButton = styled(SubmitButton)`
  && {
    min-height: 4rem;
    margin-top: 0;
  }
`;
