import { CardActionArea, styled } from '@mui/material';

export const SectionHeaderTitle = styled(CardActionArea)`
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  padding: 1rem;
  font-size: 1.5rem;
  font-weight: bold;
  height: 3.7rem;
`;
