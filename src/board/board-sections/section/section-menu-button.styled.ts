import { styled } from '@mui/material';
import { IconButton } from '../../../common/buttons';

export const SectionMenuButton = styled(IconButton)`
  font-size: 1.6rem;
  margin-left: 1rem;
  align-self: self-start;
  margin-top: 0.4rem;
`;
