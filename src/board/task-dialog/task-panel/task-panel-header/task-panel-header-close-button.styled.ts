import { styled } from '@mui/material';
import { IconButton } from '../../../../common/buttons';

export const TaskPanelHeaderCloseButton = styled(IconButton)`
  margin-left: 2rem;
  margin-top: 0.429rem;
`;
