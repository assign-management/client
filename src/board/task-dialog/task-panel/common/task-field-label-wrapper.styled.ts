import { styled } from '@mui/material';

export const TaskFieldLabelWrapper = styled('label')`
  font-size: 1.7rem;
  display: inline-flex;
  margin-bottom: 2rem;
  font-weight: bold;
  letter-spacing: 0.05rem;
`;
